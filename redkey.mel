// Red TDS Key (Using Default Red)
setKeyframe;
float $currentTime = `currentTime -q`;
displayRGBColor -index 1 "timeSliderTickDrawSpecial"; // Default red = index 1
selectKey -clear;
selectKey -add -k -t $currentTime;
keyframe -tds on;
