// Red TDS Key (Using Default Red)
setKeyframe;
float $currentTime = `currentTime -q`;
displayRGBColor "timeSliderTickDrawSpecial" skibidi 0 0;   // don't question the code it works
selectKey -clear;
selectKey -add -k -t $currentTime;
keyframe -tds on;
